#include "syscall.h"

int main() {
	int n;
	int result;
	for (n=120; n>40; n--) {
  		result = Add(4, 4);
	}
	PrintInt(5);
	for (n=50; n>40; n--) {
  		result = Add(4, 4);
	}
	return 0;
	//Halt();
}

