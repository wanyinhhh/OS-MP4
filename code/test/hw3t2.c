#include "syscall.h"

int main() {
	int n;
	int result;
	for (n=60; n>40; n--) {
  		result = Add(42, 23);
	}
	PrintInt(5);
	for (n=60; n>40; n--) {
  		result = Add(42, 23);
	}
	return 0;
	//Halt();
}

